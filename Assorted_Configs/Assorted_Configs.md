This directory holds various configuration files that would need to be applied after installing certain applications.  

These would ideally be included in a dotfiles collection, to me applied automatically after applications are installed.
